// Pig Latin translation program
// Author: Svitlana Slonska
// Date: 07.03.2023

//Creating function to translate text to Pig Latine
function translatePigLatine(str) {
    //Creating array with vowels
    let vowel = ['a', 'e', 'i', 'o', 'u'];
    //Creating variable result
    result = str.splite("");

    if (vowels.includes(str.charAt(0))) {
        return str += "way";
    }
}

console.log(translatePigLatine("eight"));